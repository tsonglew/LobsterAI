diff --git a/src/agents/pi-embedded-runner/replay-history.ts b/src/agents/pi-embedded-runner/replay-history.ts
index a96541c..6482eec 100644
--- a/src/agents/pi-embedded-runner/replay-history.ts
+++ b/src/agents/pi-embedded-runner/replay-history.ts
@@ -28,7 +28,11 @@ import {
   sanitizeToolUseResultPairing,
   stripToolResultDetails,
 } from "../session-transcript-repair.js";
-import { sanitizeToolCallIdsForCloudCodeAssist } from "../tool-call-id.js";
+import {
+  extractToolCallsFromAssistant,
+  extractToolResultId,
+  sanitizeToolCallIdsForCloudCodeAssist,
+} from "../tool-call-id.js";
 import type { TranscriptPolicy } from "../transcript-policy.js";
 import {
   resolveTranscriptPolicy,
@@ -44,6 +48,9 @@ import { dropThinkingBlocks } from "./thinking.js";
 
 const INTER_SESSION_PREFIX_BASE = "[Inter-session message]";
 const MODEL_SNAPSHOT_CUSTOM_TYPE = "model-snapshot";
+const MAX_PRESERVED_ABORTED_TOOL_HISTORY_PAIRS = 3;
+const ABORTED_TOOL_HISTORY_TEXT_RE =
+  /^(?:aborted|operation (?:was )?aborted|request was aborted|cancelled|canceled)$/i;
 type CustomEntryLike = { type?: unknown; customType?: unknown; data?: unknown };
 type ModelSnapshotEntry = {
   timestamp: number;
@@ -373,6 +380,143 @@ function isSameModelSnapshot(a: ModelSnapshotEntry, b: ModelSnapshotEntry): bool
   );
 }
 
+function getMessageRole(message: AgentMessage | undefined): string | undefined {
+  if (!message || typeof message !== "object") {
+    return undefined;
+  }
+  return (message as { role?: unknown }).role as string | undefined;
+}
+
+function getTextFromContent(content: unknown): string {
+  if (typeof content === "string") {
+    return content;
+  }
+  if (!Array.isArray(content)) {
+    return "";
+  }
+  return content
+    .map((block) => {
+      if (typeof block === "string") {
+        return block;
+      }
+      if (!block || typeof block !== "object") {
+        return "";
+      }
+      const text = (block as { text?: unknown }).text;
+      return typeof text === "string" ? text : "";
+    })
+    .join("\n")
+    .trim();
+}
+
+function hasAssistantVisibleText(message: Extract<AgentMessage, { role: "assistant" }>): boolean {
+  return getTextFromContent((message as { content?: unknown }).content).trim().length > 0;
+}
+
+function getToolOnlyAssistantCallIds(message: AgentMessage): Set<string> | null {
+  if (getMessageRole(message) !== "assistant") {
+    return null;
+  }
+  const assistant = message as Extract<AgentMessage, { role: "assistant" }>;
+  if (hasAssistantVisibleText(assistant)) {
+    return null;
+  }
+  const toolCalls = extractToolCallsFromAssistant(assistant);
+  if (toolCalls.length === 0) {
+    return null;
+  }
+  return new Set(toolCalls.map((call) => call.id).filter(Boolean));
+}
+
+function extractReplayToolResultId(message: AgentMessage): string | null {
+  if (getMessageRole(message) === "toolResult") {
+    return extractToolResultId(message as Extract<AgentMessage, { role: "toolResult" }>);
+  }
+  const record = message as unknown as Record<string, unknown>;
+  for (const value of [
+    record.toolUseId,
+    record.toolCallId,
+    record.tool_use_id,
+    record.tool_call_id,
+    record.callId,
+    record.call_id,
+  ]) {
+    if (typeof value === "string" && value.trim()) {
+      return value.trim();
+    }
+  }
+  return null;
+}
+
+function isAbortedToolResultMessage(message: AgentMessage): boolean {
+  const role = getMessageRole(message);
+  if (role !== "toolResult" && role !== "tool") {
+    return false;
+  }
+  return ABORTED_TOOL_HISTORY_TEXT_RE.test(
+    getTextFromContent((message as { content?: unknown }).content).trim(),
+  );
+}
+
+function sanitizeAbortedToolLoopHistory(messages: AgentMessage[]): AgentMessage[] {
+  const pairs: Array<{ start: number; endExclusive: number }> = [];
+
+  for (let index = 0; index < messages.length; index += 1) {
+    const message = messages[index];
+    if (!message) {
+      continue;
+    }
+    const expectedIds = getToolOnlyAssistantCallIds(message);
+    if (!expectedIds || expectedIds.size === 0) {
+      continue;
+    }
+
+    const seenIds = new Set<string>();
+    let nextIndex = index + 1;
+    let toolResultCount = 0;
+    let allResultsAborted = true;
+    while (nextIndex < messages.length) {
+      const candidate = messages[nextIndex];
+      if (!candidate) {
+        break;
+      }
+      const role = getMessageRole(candidate);
+      if (role !== "toolResult" && role !== "tool") {
+        break;
+      }
+      toolResultCount += 1;
+      if (!isAbortedToolResultMessage(candidate)) {
+        allResultsAborted = false;
+        break;
+      }
+      const toolResultId = extractReplayToolResultId(candidate);
+      if (toolResultId) {
+        seenIds.add(toolResultId);
+      }
+      nextIndex += 1;
+    }
+
+    const allExpectedIdsSeen = [...expectedIds].every((id) => seenIds.has(id));
+    if (toolResultCount > 0 && allResultsAborted && allExpectedIdsSeen) {
+      pairs.push({ start: index, endExclusive: nextIndex });
+      index = nextIndex - 1;
+    }
+  }
+
+  if (pairs.length <= MAX_PRESERVED_ABORTED_TOOL_HISTORY_PAIRS) {
+    return messages;
+  }
+
+  const dropIndexes = new Set<number>();
+  for (const pair of pairs.slice(0, -MAX_PRESERVED_ABORTED_TOOL_HISTORY_PAIRS)) {
+    for (let index = pair.start; index < pair.endExclusive; index += 1) {
+      dropIndexes.add(index);
+    }
+  }
+
+  return messages.filter((_, index) => !dropIndexes.has(index));
+}
+
 /**
  * Applies the generic replay-history cleanup pipeline before provider-owned
  * replay hooks run.
@@ -412,8 +556,9 @@ export async function sanitizeSessionHistory(params: {
     params.modelApi === "openai-responses" ||
     params.modelApi === "openai-codex-responses" ||
     params.modelApi === "azure-openai-responses";
+  const sanitizedAbortedToolLoops = sanitizeAbortedToolLoopHistory(withInterSessionMarkers);
   const sanitizedImages = await sanitizeSessionMessagesImages(
-    withInterSessionMarkers,
+    sanitizedAbortedToolLoops,
     "session:history",
     {
       sanitizeMode: policy.sanitizeMode,
diff --git a/src/agents/pi-embedded-runner/sanitize-session-history.tool-result-details.test.ts b/src/agents/pi-embedded-runner/sanitize-session-history.tool-result-details.test.ts
index 5c93c07..c08d654 100644
--- a/src/agents/pi-embedded-runner/sanitize-session-history.tool-result-details.test.ts
+++ b/src/agents/pi-embedded-runner/sanitize-session-history.tool-result-details.test.ts
@@ -2,7 +2,10 @@ import type { AgentMessage } from "@mariozechner/pi-agent-core";
 import type { ToolResultMessage, UserMessage } from "@mariozechner/pi-ai";
 import { SessionManager } from "@mariozechner/pi-coding-agent";
 import { describe, expect, it, vi } from "vitest";
-import { makeAgentAssistantMessage } from "../test-helpers/agent-message-fixtures.js";
+import {
+  makeAgentAssistantMessage,
+  makeAgentToolResultMessage,
+} from "../test-helpers/agent-message-fixtures.js";
 import { sanitizeSessionHistory } from "./replay-history.js";
 
 vi.mock("../../plugins/provider-runtime.js", () => ({
@@ -12,6 +15,53 @@ vi.mock("../../plugins/provider-runtime.js", () => ({
 }));
 
 describe("sanitizeSessionHistory toolResult details stripping", () => {
+  it("drops old aborted tool-only turns while preserving recent diagnostic pairs", async () => {
+    const sm = SessionManager.inMemory();
+
+    const messages: AgentMessage[] = [];
+    for (let i = 0; i < 10; i += 1) {
+      messages.push(
+        makeAgentAssistantMessage({
+          content: [
+            { type: "toolCall", id: `call_${i}`, name: "exec", arguments: { command: "dir" } },
+          ],
+          model: "gpt-5.4",
+          stopReason: "toolUse",
+          timestamp: i * 2,
+        }),
+        makeAgentToolResultMessage({
+          toolCallId: `call_${i}`,
+          toolName: "exec",
+          content: [{ type: "text", text: "Aborted" }],
+          isError: true,
+          timestamp: i * 2 + 1,
+        }),
+      );
+    }
+    messages.push({
+      role: "user",
+      content: "continue",
+      timestamp: 30,
+    } satisfies UserMessage);
+
+    const sanitized = await sanitizeSessionHistory({
+      messages,
+      modelApi: "anthropic-messages",
+      provider: "anthropic",
+      modelId: "claude-opus-4-6",
+      sessionManager: sm,
+      sessionId: "test",
+    });
+
+    expect(sanitized).toHaveLength(7);
+    const serialized = JSON.stringify(sanitized);
+    expect(serialized).not.toContain("call0");
+    expect(serialized).not.toContain("call6");
+    expect(serialized).toContain("call7");
+    expect(serialized).toContain("call8");
+    expect(serialized).toContain("call9");
+  });
+
   it("strips toolResult.details so untrusted payloads are not fed back to the model", async () => {
     const sm = SessionManager.inMemory();
 
diff --git a/src/agents/pi-tools.before-tool-call.e2e.test.ts b/src/agents/pi-tools.before-tool-call.e2e.test.ts
index 5c9125a..6648908 100644
--- a/src/agents/pi-tools.before-tool-call.e2e.test.ts
+++ b/src/agents/pi-tools.before-tool-call.e2e.test.ts
@@ -10,7 +10,12 @@ import {
   runBeforeToolCallHook,
   wrapToolWithBeforeToolCallHook,
 } from "./pi-tools.before-tool-call.js";
-import { CRITICAL_THRESHOLD, GLOBAL_CIRCUIT_BREAKER_THRESHOLD } from "./tool-loop-detection.js";
+import {
+  ABORTED_TOOL_LOOP_CRITICAL_THRESHOLD,
+  ABORTED_TOOL_LOOP_TOTAL_THRESHOLD,
+  CRITICAL_THRESHOLD,
+  GLOBAL_CIRCUIT_BREAKER_THRESHOLD,
+} from "./tool-loop-detection.js";
 import type { AnyAgentTool } from "./tools/common.js";
 import { callGatewayTool } from "./tools/gateway.js";
 
@@ -135,6 +140,17 @@ describe("before_tool_call loop detection behavior", () => {
     };
   }
 
+  function createAbortedExecFixture() {
+    const execute = vi.fn().mockResolvedValue({
+      content: [{ type: "text", text: "Aborted" }],
+      details: { ok: false },
+    });
+    return {
+      tool: createWrappedTool("exec", execute),
+      params: { command: "dir" },
+    };
+  }
+
   function createNoProgressProcessFixture(sessionId: string) {
     const execute = vi.fn().mockResolvedValue({
       content: [{ type: "text", text: "(no new output)\n\nProcess still running." }],
@@ -226,6 +242,36 @@ describe("before_tool_call loop detection behavior", () => {
     ).rejects.toThrow("global circuit breaker");
   });
 
+  it("blocks repeated aborted tool results before the global breaker threshold", async () => {
+    const { tool, params } = createAbortedExecFixture();
+
+    for (let i = 0; i < ABORTED_TOOL_LOOP_CRITICAL_THRESHOLD; i += 1) {
+      await expect(tool.execute(`abort-${i}`, params, undefined, undefined)).resolves.toBeDefined();
+    }
+
+    await expect(
+      tool.execute(`abort-${ABORTED_TOOL_LOOP_CRITICAL_THRESHOLD}`, params, undefined, undefined),
+    ).rejects.toThrow("aborted outcomes");
+  });
+
+  it("blocks excessive aborted tool results across different signatures", async () => {
+    const execute = vi.fn().mockResolvedValue({
+      content: [{ type: "text", text: "Operation aborted" }],
+      details: { ok: false },
+    });
+    const tool = createWrappedTool("exec", execute);
+
+    for (let i = 0; i < ABORTED_TOOL_LOOP_TOTAL_THRESHOLD; i += 1) {
+      await expect(
+        tool.execute(`abort-total-${i}`, { command: `dir ${i}` }, undefined, undefined),
+      ).resolves.toBeDefined();
+    }
+
+    await expect(
+      tool.execute("abort-total-final", { command: "dir final" }, undefined, undefined),
+    ).rejects.toThrow("aborted outcomes");
+  });
+
   it("coalesces repeated generic warning events into threshold buckets", async () => {
     await withToolLoopEvents(
       async (emitted) => {
diff --git a/src/agents/tool-loop-detection.ts b/src/agents/tool-loop-detection.ts
index a239ff6..530b81c 100644
--- a/src/agents/tool-loop-detection.ts
+++ b/src/agents/tool-loop-detection.ts
@@ -10,6 +10,7 @@ export type LoopDetectorKind =
   | "generic_repeat"
   | "unknown_tool_repeat"
   | "known_poll_no_progress"
+  | "aborted_tool_loop"
   | "global_circuit_breaker"
   | "ping_pong";
 
@@ -30,6 +31,10 @@ export const WARNING_THRESHOLD = 10;
 export const UNKNOWN_TOOL_THRESHOLD = 10;
 export const CRITICAL_THRESHOLD = 20;
 export const GLOBAL_CIRCUIT_BREAKER_THRESHOLD = 30;
+export const ABORTED_TOOL_LOOP_WARNING_THRESHOLD = 5;
+export const ABORTED_TOOL_LOOP_CRITICAL_THRESHOLD = 8;
+export const ABORTED_TOOL_LOOP_TOTAL_THRESHOLD = 20;
+const ABORTED_OUTCOME_RE = /\b(?:aborterror|aborted|operation aborted|request was aborted|operation was aborted|cancelled|canceled)\b/i;
 const DEFAULT_LOOP_DETECTION_CONFIG = {
   enabled: false,
   historySize: TOOL_CALL_HISTORY_SIZE,
@@ -190,6 +195,18 @@ function formatErrorForHash(error: unknown): string {
   return stableStringify(error);
 }
 
+function normalizeAbortedOutcomeText(text: string): string | undefined {
+  const normalized = text.replace(/\s+/g, " ").trim();
+  if (!normalized || !ABORTED_OUTCOME_RE.test(normalized)) {
+    return undefined;
+  }
+  return "aborted";
+}
+
+function isAbortedOutcomeHash(resultHash?: string): boolean {
+  return typeof resultHash === "string" && resultHash.startsWith("aborted:");
+}
+
 function extractUnknownToolName(error: unknown): string | undefined {
   const raw = formatErrorForHash(error).trim();
   if (!raw) {
@@ -210,17 +227,32 @@ function hashToolOutcome(
 ): { resultHash?: string; unknownToolName?: string } {
   if (error !== undefined) {
     const unknownToolName = extractUnknownToolName(error);
+    const errorText = formatErrorForHash(error);
+    const abortedOutcome = normalizeAbortedOutcomeText(errorText);
     return {
-      resultHash: `error:${digestStable(formatErrorForHash(error))}`,
+      resultHash: abortedOutcome ? `aborted:${abortedOutcome}` : `error:${digestStable(errorText)}`,
       unknownToolName,
     };
   }
   if (!isPlainObject(result)) {
-    return { resultHash: result === undefined ? undefined : digestStable(result) };
+    if (result === undefined) {
+      return { resultHash: undefined };
+    }
+    if (typeof result === "string") {
+      const abortedOutcome = normalizeAbortedOutcomeText(result);
+      if (abortedOutcome) {
+        return { resultHash: `aborted:${abortedOutcome}` };
+      }
+    }
+    return { resultHash: digestStable(result) };
   }
 
   const details = isPlainObject(result.details) ? result.details : {};
   const text = extractTextContent(result);
+  const abortedOutcome = normalizeAbortedOutcomeText(text);
+  if (abortedOutcome) {
+    return { resultHash: `aborted:${abortedOutcome}` };
+  }
   if (isKnownPollToolCall(toolName, params) && toolName === "process" && isPlainObject(params)) {
     const action = params.action;
     if (action === "poll") {
@@ -315,6 +347,36 @@ function getNoProgressStreak(
   return { count: streak, latestResultHash };
 }
 
+function getAbortedToolLoopStats(
+  history: Array<{ toolName: string; argsHash: string; resultHash?: string }>,
+  toolName: string,
+  argsHash: string,
+): { signatureStreak: number; total: number } {
+  let signatureStreak = 0;
+  let total = 0;
+  let signatureStreakOpen = true;
+
+  for (let i = history.length - 1; i >= 0; i -= 1) {
+    const record = history[i];
+    if (!record) {
+      continue;
+    }
+    const aborted = isAbortedOutcomeHash(record.resultHash);
+    if (aborted) {
+      total += 1;
+    }
+    if (signatureStreakOpen && record.toolName === toolName && record.argsHash === argsHash) {
+      if (!aborted) {
+        signatureStreakOpen = false;
+      } else {
+        signatureStreak += 1;
+      }
+    }
+  }
+
+  return { signatureStreak, total };
+}
+
 function getPingPongStreak(
   history: Array<{ toolName: string; argsHash: string; resultHash?: string }>,
   currentSignature: string,
@@ -441,6 +503,7 @@ export function detectToolCallLoop(
   const noProgress = getNoProgressStreak(history, toolName, currentHash);
   const noProgressStreak = noProgress.count;
   const knownPollTool = isKnownPollToolCall(toolName, params);
+  const abortedLoop = getAbortedToolLoopStats(history, toolName, currentHash);
   const pingPong = getPingPongStreak(history, currentHash);
 
   if (unknownToolStreak.count >= resolvedConfig.unknownToolThreshold) {
@@ -454,6 +517,48 @@ export function detectToolCallLoop(
     };
   }
 
+  if (abortedLoop.total >= ABORTED_TOOL_LOOP_TOTAL_THRESHOLD) {
+    log.error(
+      `Aborted tool loop breaker triggered: ${abortedLoop.total} aborted tool outcomes recorded`,
+    );
+    return {
+      stuck: true,
+      level: "critical",
+      detector: "aborted_tool_loop",
+      count: abortedLoop.total,
+      message: `CRITICAL: tool calls have returned aborted outcomes ${abortedLoop.total} times. Session execution blocked to prevent runaway retry loops.`,
+      warningKey: `aborted-total:${abortedLoop.total}`,
+    };
+  }
+
+  if (abortedLoop.signatureStreak >= ABORTED_TOOL_LOOP_CRITICAL_THRESHOLD) {
+    log.error(
+      `Critical aborted tool loop detected: ${toolName} repeated ${abortedLoop.signatureStreak} times`,
+    );
+    return {
+      stuck: true,
+      level: "critical",
+      detector: "aborted_tool_loop",
+      count: abortedLoop.signatureStreak,
+      message: `CRITICAL: ${toolName} has returned aborted outcomes ${abortedLoop.signatureStreak} times for identical arguments. Session execution blocked to prevent runaway retry loops.`,
+      warningKey: `aborted:${toolName}:${currentHash}:${noProgress.latestResultHash ?? "aborted"}`,
+    };
+  }
+
+  if (abortedLoop.signatureStreak >= ABORTED_TOOL_LOOP_WARNING_THRESHOLD) {
+    log.warn(
+      `Aborted tool loop warning: ${toolName} repeated ${abortedLoop.signatureStreak} times`,
+    );
+    return {
+      stuck: true,
+      level: "warning",
+      detector: "aborted_tool_loop",
+      count: abortedLoop.signatureStreak,
+      message: `WARNING: ${toolName} has returned aborted outcomes ${abortedLoop.signatureStreak} times for identical arguments. Stop retrying the same call and choose a different approach or report the task as failed.`,
+      warningKey: `aborted:${toolName}:${currentHash}:${noProgress.latestResultHash ?? "aborted"}`,
+    };
+  }
+
   if (noProgressStreak >= resolvedConfig.globalCircuitBreakerThreshold) {
     log.error(
       `Global circuit breaker triggered: ${toolName} repeated ${noProgressStreak} times with no progress`,

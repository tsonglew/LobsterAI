diff --git a/src/gateway/server-methods/chat.ts b/src/gateway/server-methods/chat.ts
index 56bd856f..960310d8 100644
--- a/src/gateway/server-methods/chat.ts
+++ b/src/gateway/server-methods/chat.ts
@@ -1837,6 +1837,17 @@ export const chatHandlers: GatewayRequestHandlers = {
     }
     const rawSessionKey = p.sessionKey;
     const { cfg, entry, canonicalKey: sessionKey } = loadSessionEntry(rawSessionKey);
+    const clientRunId = p.idempotencyKey;
+    const firstResponseTimingStartedAt = Date.now();
+    const logFirstResponseTiming = (stage: string, extra = "") => {
+      context.logGateway.debug(
+        `[OpenClawFirstResponseTiming] ${stage}: runId=${clientRunId} sessionKey=${sessionKey} elapsedMs=${Date.now() - firstResponseTimingStartedAt}${extra}`,
+      );
+    };
+    logFirstResponseTiming(
+      "chat.send accepted",
+      ` messageChars=${inboundMessage.length} attachments=${normalizedAttachments.length}`,
+    );
     const agentId = resolveSessionAgentId({
       sessionKey,
       config: cfg,
@@ -1850,7 +1861,6 @@ export const chatHandlers: GatewayRequestHandlers = {
       overrideMs: p.timeoutMs,
     });
     const now = Date.now();
-    const clientRunId = p.idempotencyKey;
 
     const sendPolicy = resolveSendPolicy({
       cfg,
@@ -1947,6 +1957,7 @@ export const chatHandlers: GatewayRequestHandlers = {
         status: "started" as const,
       };
       respond(true, ackPayload, undefined, { runId: clientRunId });
+      logFirstResponseTiming("chat.send ack sent");
       const persistedImagesPromise = persistChatSendImages({
         images: parsedImages,
         imageOrder,
@@ -2141,6 +2152,7 @@ export const chatHandlers: GatewayRequestHandlers = {
       });
 
       let agentRunStarted = false;
+      logFirstResponseTiming("dispatch inbound message started");
       void dispatchInboundMessage({
         ctx,
         cfg,
@@ -2175,6 +2187,10 @@ export const chatHandlers: GatewayRequestHandlers = {
         },
       })
         .then(async () => {
+          logFirstResponseTiming(
+            "dispatch inbound message resolved",
+            ` agentRunStarted=${agentRunStarted}`,
+          );
           await rewriteUserTranscriptMedia();
           if (!agentRunStarted) {
             await emitUserTranscriptUpdate();
@@ -2262,6 +2278,7 @@ export const chatHandlers: GatewayRequestHandlers = {
           });
         })
         .catch((err) => {
+          logFirstResponseTiming("dispatch inbound message failed", ` error=${formatForLog(err)}`);
           void rewriteUserTranscriptMedia().catch((rewriteErr) => {
             context.logGateway.warn(
               `webchat transcript media rewrite failed after error: ${formatForLog(rewriteErr)}`,
@@ -2295,6 +2312,7 @@ export const chatHandlers: GatewayRequestHandlers = {
           });
         })
         .finally(() => {
+          logFirstResponseTiming("chat.send cleanup finished");
           context.chatAbortControllers.delete(clientRunId);
         });
     } catch (err) {
diff --git a/src/auto-reply/reply/get-reply.ts b/src/auto-reply/reply/get-reply.ts
index 20c82e29..a8109e19 100644
--- a/src/auto-reply/reply/get-reply.ts
+++ b/src/auto-reply/reply/get-reply.ts
@@ -11,6 +11,7 @@ import { resolveAgentTimeoutMs } from "../../agents/timeout.js";
 import { DEFAULT_AGENT_WORKSPACE_DIR, ensureAgentWorkspace } from "../../agents/workspace.js";
 import { resolveChannelModelOverride } from "../../channels/model-overrides.js";
 import { type OpenClawConfig, loadConfig } from "../../config/config.js";
+import { logVerbose } from "../../globals.js";
 import { defaultRuntime } from "../../runtime.js";
 import { normalizeOptionalString } from "../../shared/string-coerce.js";
 import { normalizeStringEntries } from "../../shared/string-normalization.js";
@@ -73,6 +74,20 @@ function loadOriginRouting() {
   return originRoutingPromise;
 }
 
+function logFirstResponseTiming(params: {
+  stage: string;
+  startedAt: number;
+  runId?: string;
+  sessionKey?: string;
+  extra?: string;
+}) {
+  logVerbose(
+    `[OpenClawFirstResponseTiming] ${params.stage}: runId=${params.runId ?? "unknown"} ` +
+      `sessionKey=${params.sessionKey ?? "unknown"} elapsedMs=${Date.now() - params.startedAt}` +
+      (params.extra ? ` ${params.extra}` : ""),
+  );
+}
+
 function mergeSkillFilters(channelFilter?: string[], agentFilter?: string[]): string[] | undefined {
   const normalize = (list?: string[]) => {
     if (!Array.isArray(list)) {
@@ -149,6 +164,15 @@ export async function getReplyFromConfig(
   opts?: GetReplyOptions,
   configOverride?: OpenClawConfig,
 ): Promise<ReplyPayload | ReplyPayload[] | undefined> {
+  const firstResponseTimingStartedAt = Date.now();
+  const firstResponseRunId = opts?.runId ?? ctx.MessageSidFull ?? ctx.MessageSid;
+  const firstResponseSessionKey = ctx.SessionKey;
+  logFirstResponseTiming({
+    stage: "getReplyFromConfig started",
+    startedAt: firstResponseTimingStartedAt,
+    runId: firstResponseRunId,
+    sessionKey: firstResponseSessionKey,
+  });
   const isFastTestEnv = process.env.OPENCLAW_TEST_FAST === "1";
   const cfg = resolveGetReplyConfig({
     loadConfig,
@@ -216,6 +240,13 @@ export async function getReplyFromConfig(
         ensureBootstrapFiles: !agentCfg?.skipBootstrap && !isFastTestEnv,
       });
   const workspaceDir = workspace.dir;
+  logFirstResponseTiming({
+    stage: "agent workspace ready",
+    startedAt: firstResponseTimingStartedAt,
+    runId: firstResponseRunId,
+    sessionKey: firstResponseSessionKey,
+    extra: `workspaceChars=${workspaceDir.length}`,
+  });
   const runCwd = resolveAgentRunCwd(cfg, agentId, resolvedOpts?.cwd);
   const agentDir = resolveAgentDir(cfg, agentId);
   const timeoutMs = resolveAgentTimeoutMs({ cfg, overrideSeconds: opts?.timeoutOverrideSeconds });
@@ -246,11 +277,23 @@ export async function getReplyFromConfig(
       cfg,
     });
   }
+  logFirstResponseTiming({
+    stage: "inbound understanding finished",
+    startedAt: firstResponseTimingStartedAt,
+    runId: firstResponseRunId,
+    sessionKey: firstResponseSessionKey,
+  });
   emitPreAgentMessageHooks({
     ctx: finalized,
     cfg,
     isFastTestEnv,
   });
+  logFirstResponseTiming({
+    stage: "pre-agent message hooks emitted",
+    startedAt: firstResponseTimingStartedAt,
+    runId: firstResponseRunId,
+    sessionKey: firstResponseSessionKey,
+  });
 
   const commandAuthorized = finalized.CommandAuthorized;
   const sessionState = useFastTestBootstrap
@@ -262,10 +305,16 @@ export async function getReplyFromConfig(
         workspaceDir,
       })
     : await initSessionState({
-        ctx: finalized,
-        cfg,
-        commandAuthorized,
-      });
+    ctx: finalized,
+    cfg,
+    commandAuthorized,
+  });
+  logFirstResponseTiming({
+    stage: "session state initialized",
+    startedAt: firstResponseTimingStartedAt,
+    runId: firstResponseRunId,
+    sessionKey: firstResponseSessionKey,
+  });
   let {
     sessionCtx,
     sessionEntry,
@@ -451,6 +500,12 @@ export async function getReplyFromConfig(
   if (directiveResult.kind === "reply") {
     return directiveResult.reply;
   }
+  logFirstResponseTiming({
+    stage: "reply directives resolved",
+    startedAt: firstResponseTimingStartedAt,
+    runId: firstResponseRunId,
+    sessionKey,
+  });
 
   let {
     commandSource,
@@ -580,6 +635,12 @@ export async function getReplyFromConfig(
       }
     }
   }
+  logFirstResponseTiming({
+    stage: "before_agent_reply hooks finished",
+    startedAt: firstResponseTimingStartedAt,
+    runId: firstResponseRunId,
+    sessionKey,
+  });
 
   if (!useFastTestBootstrap && sessionKey && hasInboundMedia(ctx)) {
     const { stageSandboxMedia } = await loadStageSandboxMediaRuntime();
@@ -591,7 +652,20 @@ export async function getReplyFromConfig(
       workspaceDir,
     });
   }
+  logFirstResponseTiming({
+    stage: "sandbox media staging finished",
+    startedAt: firstResponseTimingStartedAt,
+    runId: firstResponseRunId,
+    sessionKey,
+  });
 
+  logFirstResponseTiming({
+    stage: "runPreparedReply starting",
+    startedAt: firstResponseTimingStartedAt,
+    runId: firstResponseRunId,
+    sessionKey,
+    extra: `provider=${provider}/${model}`,
+  });
   return runPreparedReply({
     ctx,
     sessionCtx,
diff --git a/src/auto-reply/reply/dispatch-from-config.ts b/src/auto-reply/reply/dispatch-from-config.ts
index 79049eea..8cb3fbcd 100644
--- a/src/auto-reply/reply/dispatch-from-config.ts
+++ b/src/auto-reply/reply/dispatch-from-config.ts
@@ -202,6 +202,16 @@ export async function dispatchReplyFromConfig(
   params: DispatchFromConfigParams,
 ): Promise<DispatchFromConfigResult> {
   const { ctx, cfg, dispatcher } = params;
+  const firstResponseTimingStartedAt = Date.now();
+  const firstResponseRunId = params.replyOptions?.runId ?? ctx.MessageSidFull ?? ctx.MessageSid;
+  const logFirstResponseTiming = (stage: string, extra = "") => {
+    logVerbose(
+      `[OpenClawFirstResponseTiming] ${stage}: runId=${firstResponseRunId ?? "unknown"} ` +
+        `sessionKey=${ctx.SessionKey ?? "unknown"} ` +
+        `elapsedMs=${Date.now() - firstResponseTimingStartedAt}${extra}`,
+    );
+  };
+  logFirstResponseTiming("dispatchReplyFromConfig started");
   const diagnosticsEnabled = isDiagnosticsEnabled(cfg);
   const channel = normalizeLowercaseStringOrEmpty(ctx.Surface ?? ctx.Provider ?? "unknown");
   const chatId = ctx.To ?? ctx.From;
@@ -849,6 +859,10 @@ export async function dispatchReplyFromConfig(
 
     const replyResolver =
       params.replyResolver ?? (await loadGetReplyFromConfigRuntime()).getReplyFromConfig;
+    logFirstResponseTiming(
+      "reply resolver ready",
+      ` customResolver=${params.replyResolver ? "true" : "false"}`,
+    );
     const replyResult = await replyResolver(
       ctx,
       {
@@ -958,6 +972,10 @@ export async function dispatchReplyFromConfig(
       },
       params.configOverride,
     );
+    logFirstResponseTiming(
+      "reply resolver finished",
+      ` returned=${replyResult === undefined ? "false" : "true"}`,
+    );
 
     if (ctx.AcpDispatchTailAfterReset === true) {
       // Command handling prepared a trailing prompt after ACP in-place reset.
diff --git a/src/auto-reply/reply/get-reply-run.ts b/src/auto-reply/reply/get-reply-run.ts
index a0ab2233..13fa0ecd 100644
--- a/src/auto-reply/reply/get-reply-run.ts
+++ b/src/auto-reply/reply/get-reply-run.ts
@@ -187,9 +187,31 @@ type RunPreparedReplyParams = {
   abortedLastRun: boolean;
 };
 
+function logFirstResponseTiming(params: {
+  stage: string;
+  startedAt: number;
+  runId?: string;
+  sessionKey?: string;
+  extra?: string;
+}) {
+  logVerbose(
+    `[OpenClawFirstResponseTiming] ${params.stage}: runId=${params.runId ?? "unknown"} ` +
+      `sessionKey=${params.sessionKey ?? "unknown"} elapsedMs=${Date.now() - params.startedAt}` +
+      (params.extra ? ` ${params.extra}` : ""),
+  );
+}
+
 export async function runPreparedReply(
   params: RunPreparedReplyParams,
 ): Promise<ReplyPayload | ReplyPayload[] | undefined> {
+  const firstResponseTimingStartedAt = Date.now();
+  const firstResponseRunId = params.opts?.runId ?? params.sessionCtx.MessageSidFull ?? params.sessionCtx.MessageSid;
+  logFirstResponseTiming({
+    stage: "runPreparedReply started",
+    startedAt: firstResponseTimingStartedAt,
+    runId: firstResponseRunId,
+    sessionKey: params.sessionKey,
+  });
   const {
     ctx,
     sessionCtx,
@@ -329,6 +351,12 @@ export async function runPreparedReply(
           cfg,
         })
       : null;
+  logFirstResponseTiming({
+    stage: "prepared base prompt context",
+    startedAt: firstResponseTimingStartedAt,
+    runId: firstResponseRunId,
+    sessionKey,
+  });
   const baseBodyFinal = isBareSessionReset
     ? buildBareSessionResetPrompt(cfg)
     : stripPromptThinkingDirectives(baseBody);
@@ -449,6 +477,13 @@ export async function runPreparedReply(
             skillFilter: opts?.skillFilter,
           });
         })();
+  logFirstResponseTiming({
+    stage: "skill snapshot ready",
+    startedAt: firstResponseTimingStartedAt,
+    runId: firstResponseRunId,
+    sessionKey,
+    extra: `hasSnapshot=${skillResult.skillsSnapshot ? "true" : "false"}`,
+  });
   sessionEntry = skillResult.sessionEntry ?? sessionEntry;
   currentSystemSent = skillResult.systemSent;
   const skillsSnapshot = skillResult.skillsSnapshot;
@@ -518,6 +553,13 @@ export async function runPreparedReply(
         inlineOptions: perMessageQueueOptions,
       });
   const piRuntime = useFastReplyRuntime ? null : await loadPiEmbeddedRuntime();
+  logFirstResponseTiming({
+    stage: "pi embedded runtime loaded",
+    startedAt: firstResponseTimingStartedAt,
+    runId: firstResponseRunId,
+    sessionKey,
+    extra: `fastRuntime=${useFastReplyRuntime ? "true" : "false"}`,
+  });
   const sessionLaneKey = piRuntime
     ? piRuntime.resolveEmbeddedSessionLane(sessionKey ?? sessionIdFinal)
     : undefined;
@@ -542,7 +584,20 @@ export async function runPreparedReply(
         storePath,
         isNewSession,
       });
+  logFirstResponseTiming({
+    stage: "auth profile override resolved",
+    startedAt: firstResponseTimingStartedAt,
+    runId: firstResponseRunId,
+    sessionKey,
+    extra: `hasAuthProfile=${authProfileId ? "true" : "false"}`,
+  });
   const { runReplyAgent } = await loadAgentRunnerRuntime();
+  logFirstResponseTiming({
+    stage: "agent runner runtime loaded",
+    startedAt: firstResponseTimingStartedAt,
+    runId: firstResponseRunId,
+    sessionKey,
+  });
   const queueKey = sessionKey ?? sessionIdFinal;
   preparedSessionState = resolvePreparedSessionState();
   const resolveActiveQueueSessionId = () =>
@@ -693,6 +748,13 @@ export async function runPreparedReply(
     },
   };
 
+  logFirstResponseTiming({
+    stage: "runReplyAgent starting",
+    startedAt: firstResponseTimingStartedAt,
+    runId: firstResponseRunId,
+    sessionKey,
+    extra: `queueMode=${resolvedQueue.mode} laneSize=${laneSize}`,
+  });
   return runReplyAgent({
     commandBody: prefixedCommandBody,
     followupRun,
diff --git a/src/agents/pi-embedded-runner/run.ts b/src/agents/pi-embedded-runner/run.ts
index 9a0e9841..c56331a2 100644
--- a/src/agents/pi-embedded-runner/run.ts
+++ b/src/agents/pi-embedded-runner/run.ts
@@ -130,6 +130,22 @@ type ApiKeyInfo = ResolvedProviderAuth;
 
 const MAX_SAME_MODEL_IDLE_TIMEOUT_RETRIES = 1;
 
+function logFirstResponseTiming(params: {
+  stage: string;
+  startedAt: number;
+  runId?: string;
+  sessionId?: string;
+  sessionKey?: string;
+  extra?: string;
+}) {
+  log.debug(
+    `[OpenClawFirstResponseTiming] ${params.stage}: runId=${params.runId ?? "unknown"} ` +
+      `sessionId=${params.sessionId ?? "unknown"} ` +
+      `sessionKey=${params.sessionKey ?? "unknown"} elapsedMs=${Date.now() - params.startedAt}` +
+      (params.extra ? ` ${params.extra}` : ""),
+  );
+}
+
 function buildTraceToolSummary(params: {
   toolMetas: Array<{ toolName: string; meta?: string }>;
   hadFailure: boolean;
@@ -197,6 +213,7 @@ function backfillSessionKey(params: {
 export async function runEmbeddedPiAgent(
   params: RunEmbeddedPiAgentParams,
 ): Promise<EmbeddedPiRunResult> {
+  const firstResponseTimingStartedAt = Date.now();
   // Resolve sessionKey early so all downstream consumers (hooks, LCM, compaction)
   // receive a non-null key even when callers omit it. See #60552.
   const effectiveSessionKey = backfillSessionKey({
@@ -223,6 +240,14 @@ export async function runEmbeddedPiAgent(
         : "plain"
       : "markdown");
   const isProbeSession = params.sessionId?.startsWith("probe-") ?? false;
+  logFirstResponseTiming({
+    stage: "runEmbeddedPiAgent queued",
+    startedAt: firstResponseTimingStartedAt,
+    runId: params.runId,
+    sessionId: params.sessionId,
+    sessionKey: params.sessionKey,
+    extra: `sessionLane=${sessionLane} globalLane=${globalLane}`,
+  });
 
   const throwIfAborted = () => {
     if (!params.abortSignal?.aborted) {
@@ -243,8 +268,26 @@ export async function runEmbeddedPiAgent(
   throwIfAborted();
 
   return enqueueSession(() => {
+    const sessionLaneEnteredAt = Date.now();
+    logFirstResponseTiming({
+      stage: "session lane entered",
+      startedAt: firstResponseTimingStartedAt,
+      runId: params.runId,
+      sessionId: params.sessionId,
+      sessionKey: params.sessionKey,
+      extra: `waitMs=${sessionLaneEnteredAt - firstResponseTimingStartedAt}`,
+    });
     throwIfAborted();
     return enqueueGlobal(async () => {
+      const globalLaneEnteredAt = Date.now();
+      logFirstResponseTiming({
+        stage: "global lane entered",
+        startedAt: firstResponseTimingStartedAt,
+        runId: params.runId,
+        sessionId: params.sessionId,
+        sessionKey: params.sessionKey,
+        extra: `sessionToGlobalMs=${globalLaneEnteredAt - sessionLaneEnteredAt}`,
+      });
       throwIfAborted();
       const started = Date.now();
       const workspaceResolution = resolveRunWorkspaceDir({
@@ -267,6 +310,13 @@ export async function runEmbeddedPiAgent(
         workspaceDir: resolvedWorkspace,
         allowGatewaySubagentBinding: params.allowGatewaySubagentBinding,
       });
+      logFirstResponseTiming({
+        stage: "runtime plugins loaded",
+        startedAt: firstResponseTimingStartedAt,
+        runId: params.runId,
+        sessionId: params.sessionId,
+        sessionKey: params.sessionKey,
+      });
 
       let provider = (params.provider ?? DEFAULT_PROVIDER).trim() || DEFAULT_PROVIDER;
       let modelId = (params.model ?? DEFAULT_MODEL).trim() || DEFAULT_MODEL;
@@ -278,6 +328,13 @@ export async function runEmbeddedPiAgent(
         sessionKey: normalizedSessionKey,
       });
       await ensureOpenClawModelsJson(params.config, agentDir);
+      logFirstResponseTiming({
+        stage: "models config ready",
+        startedAt: firstResponseTimingStartedAt,
+        runId: params.runId,
+        sessionId: params.sessionId,
+        sessionKey: params.sessionKey,
+      });
       const resolvedSessionKey = normalizedSessionKey;
       const hookRunner = getGlobalHookRunner();
       const hookCtx = {
@@ -303,6 +360,14 @@ export async function runEmbeddedPiAgent(
       provider = hookSelection.provider;
       modelId = hookSelection.modelId;
       const legacyBeforeAgentStartResult = hookSelection.legacyBeforeAgentStartResult;
+      logFirstResponseTiming({
+        stage: "hook model selection finished",
+        startedAt: firstResponseTimingStartedAt,
+        runId: params.runId,
+        sessionId: params.sessionId,
+        sessionKey: params.sessionKey,
+        extra: `provider=${provider}/${modelId}`,
+      });
 
       const { model, error, authStorage, modelRegistry } = await resolveModelAsync(
         provider,
@@ -317,6 +382,13 @@ export async function runEmbeddedPiAgent(
           model: modelId,
         });
       }
+      logFirstResponseTiming({
+        stage: "model resolved",
+        startedAt: firstResponseTimingStartedAt,
+        runId: params.runId,
+        sessionId: params.sessionId,
+        sessionKey: params.sessionKey,
+      });
       let runtimeModel = model;
 
       const resolvedRuntimeModel = resolveEffectiveRuntimeModel({
@@ -430,6 +502,14 @@ export async function runEmbeddedPiAgent(
       });
 
       await initializeAuthProfile();
+      logFirstResponseTiming({
+        stage: "auth profile initialized",
+        startedAt: firstResponseTimingStartedAt,
+        runId: params.runId,
+        sessionId: params.sessionId,
+        sessionKey: params.sessionKey,
+        extra: `profiles=${profileCandidates.length}`,
+      });
       const { sessionAgentId } = resolveSessionAgentIds({
         sessionKey: params.sessionKey,
         config: params.config,
@@ -667,6 +747,14 @@ export async function runEmbeddedPiAgent(
             resolvedStreamApiKey = (apiKeyInfo as ApiKeyInfo).apiKey;
           }
 
+          logFirstResponseTiming({
+            stage: "embedded attempt starting",
+            startedAt: firstResponseTimingStartedAt,
+            runId: params.runId,
+            sessionId: params.sessionId,
+            sessionKey: params.sessionKey,
+            extra: `attempt=${runLoopIterations} provider=${provider}/${modelId}`,
+          });
           const attempt = await runEmbeddedAttemptWithBackend({
             sessionId: params.sessionId,
             sessionKey: resolvedSessionKey,
@@ -759,6 +847,14 @@ export async function runEmbeddedPiAgent(
             bootstrapPromptWarningSignature:
               bootstrapPromptWarningSignaturesSeen[bootstrapPromptWarningSignaturesSeen.length - 1],
           });
+          logFirstResponseTiming({
+            stage: "embedded attempt finished",
+            startedAt: firstResponseTimingStartedAt,
+            runId: params.runId,
+            sessionId: params.sessionId,
+            sessionKey: params.sessionKey,
+            extra: `attempt=${runLoopIterations}`,
+          });
 
           const {
             aborted,
diff --git a/src/agents/pi-embedded-runner/run/attempt.ts b/src/agents/pi-embedded-runner/run/attempt.ts
index 3694c454..d17a8997 100644
--- a/src/agents/pi-embedded-runner/run/attempt.ts
+++ b/src/agents/pi-embedded-runner/run/attempt.ts
@@ -356,9 +356,26 @@ function summarizeSessionContext(messages: AgentMessage[]): {
   };
 }
 
+function logFirstResponseTiming(params: {
+  stage: string;
+  startedAt: number;
+  runId?: string;
+  sessionId?: string;
+  sessionKey?: string;
+  extra?: string;
+}) {
+  log.debug(
+    `[OpenClawFirstResponseTiming] ${params.stage}: runId=${params.runId ?? "unknown"} ` +
+      `sessionId=${params.sessionId ?? "unknown"} ` +
+      `sessionKey=${params.sessionKey ?? "unknown"} elapsedMs=${Date.now() - params.startedAt}` +
+      (params.extra ? ` ${params.extra}` : ""),
+  );
+}
+
 export async function runEmbeddedAttempt(
   params: EmbeddedRunAttemptParams,
 ): Promise<EmbeddedRunAttemptResult> {
+  const firstResponseTimingStartedAt = Date.now();
   const resolvedWorkspace = resolveUserPath(params.workspaceDir);
   const resolvedCwd = resolveUserPath(params.cwd ?? params.workspaceDir);
   const runAbortController = new AbortController();
@@ -370,6 +387,14 @@ export async function runEmbeddedAttempt(
   log.debug(
     `embedded run start: runId=${params.runId} sessionId=${params.sessionId} provider=${params.provider} model=${params.modelId} thinking=${params.thinkLevel} messageChannel=${params.messageChannel ?? params.messageProvider ?? "unknown"}`,
   );
+  logFirstResponseTiming({
+    stage: "embedded attempt entered",
+    startedAt: firstResponseTimingStartedAt,
+    runId: params.runId,
+    sessionId: params.sessionId,
+    sessionKey: params.sessionKey,
+    extra: `provider=${params.provider}/${params.modelId}`,
+  });
 
   await fs.mkdir(resolvedWorkspace, { recursive: true });
   await fs.mkdir(resolvedCwd, { recursive: true });
@@ -393,6 +418,14 @@ export async function runEmbeddedAttempt(
     config: params.config,
     agentId: params.agentId,
   });
+  logFirstResponseTiming({
+    stage: "workspace and sandbox ready",
+    startedAt: firstResponseTimingStartedAt,
+    runId: params.runId,
+    sessionId: params.sessionId,
+    sessionKey: params.sessionKey,
+    extra: `sandbox=${sandbox?.enabled ? "true" : "false"}`,
+  });
 
   let restoreSkillEnv: (() => void) | undefined;
   try {
@@ -419,6 +452,14 @@ export async function runEmbeddedAttempt(
       workspaceDir: effectiveWorkspace,
       agentId: sessionAgentId,
     });
+    logFirstResponseTiming({
+      stage: "skills prompt resolved",
+      startedAt: firstResponseTimingStartedAt,
+      runId: params.runId,
+      sessionId: params.sessionId,
+      sessionKey: params.sessionKey,
+      extra: `skillsPromptChars=${skillsPrompt?.length ?? 0}`,
+    });
 
     const sessionLock = await acquireSessionWriteLock({
       sessionFile: params.sessionFile,
@@ -429,6 +470,13 @@ export async function runEmbeddedAttempt(
         }),
       }),
     });
+    logFirstResponseTiming({
+      stage: "session write lock acquired",
+      startedAt: firstResponseTimingStartedAt,
+      runId: params.runId,
+      sessionId: params.sessionId,
+      sessionKey: params.sessionKey,
+    });
 
     const sessionLabel = params.sessionKey ?? params.sessionId;
     const contextInjectionMode = resolveContextInjectionMode(params.config);
@@ -453,6 +501,14 @@ export async function runEmbeddedAttempt(
           runKind: params.bootstrapContextRunKind,
         }),
     });
+    logFirstResponseTiming({
+      stage: "bootstrap context resolved",
+      startedAt: firstResponseTimingStartedAt,
+      runId: params.runId,
+      sessionId: params.sessionId,
+      sessionKey: params.sessionKey,
+      extra: `bootstrapFiles=${hookAdjustedBootstrapFiles.length} contextFiles=${contextFiles.length}`,
+    });
     const bootstrapMaxChars = resolveBootstrapMaxChars(params.config);
     const bootstrapTotalMaxChars = resolveBootstrapTotalMaxChars(params.config);
     const bootstrapAnalysis = analyzeBootstrapBudget({
@@ -563,6 +619,14 @@ export async function runEmbeddedAttempt(
           }
           return allTools;
         })();
+    logFirstResponseTiming({
+      stage: "core tools created",
+      startedAt: firstResponseTimingStartedAt,
+      runId: params.runId,
+      sessionId: params.sessionId,
+      sessionKey: params.sessionKey,
+      extra: `tools=${toolsRaw.length}`,
+    });
     const toolsEnabled = supportsModelTools(params.model);
     const tools = normalizeProviderToolSchemas({
       tools: toolsEnabled ? toolsRaw : [],
@@ -583,6 +647,14 @@ export async function runEmbeddedAttempt(
           cfg: params.config,
         })
       : undefined;
+    logFirstResponseTiming({
+      stage: "bundle MCP session runtime ready",
+      startedAt: firstResponseTimingStartedAt,
+      runId: params.runId,
+      sessionId: params.sessionId,
+      sessionKey: params.sessionKey,
+      extra: `enabled=${toolsEnabled ? "true" : "false"}`,
+    });
     const bundleMcpRuntime = bundleMcpSessionRuntime
       ? await materializeBundleMcpToolsForRun({
           runtime: bundleMcpSessionRuntime,
@@ -592,6 +664,14 @@ export async function runEmbeddedAttempt(
           ],
         })
       : undefined;
+    logFirstResponseTiming({
+      stage: "bundle MCP tools materialized",
+      startedAt: firstResponseTimingStartedAt,
+      runId: params.runId,
+      sessionId: params.sessionId,
+      sessionKey: params.sessionKey,
+      extra: `tools=${bundleMcpRuntime?.tools.length ?? 0}`,
+    });
     const bundleLspRuntime = toolsEnabled
       ? await createBundleLspToolRuntime({
           workspaceDir: effectiveCwd,
@@ -603,11 +683,27 @@ export async function runEmbeddedAttempt(
           ],
         })
       : undefined;
+    logFirstResponseTiming({
+      stage: "bundle LSP tools ready",
+      startedAt: firstResponseTimingStartedAt,
+      runId: params.runId,
+      sessionId: params.sessionId,
+      sessionKey: params.sessionKey,
+      extra: `tools=${bundleLspRuntime?.tools.length ?? 0}`,
+    });
     const effectiveTools = [
       ...tools,
       ...(bundleMcpRuntime?.tools ?? []),
       ...(bundleLspRuntime?.tools ?? []),
     ];
+    logFirstResponseTiming({
+      stage: "effective tools ready",
+      startedAt: firstResponseTimingStartedAt,
+      runId: params.runId,
+      sessionId: params.sessionId,
+      sessionKey: params.sessionKey,
+      extra: `tools=${effectiveTools.length}`,
+    });
     const allowedToolNames = collectAllowedToolNames({
       tools: effectiveTools,
       clientTools,
@@ -846,6 +942,14 @@ export async function runEmbeddedAttempt(
     });
     const systemPromptOverride = createSystemPromptOverride(appendPrompt);
     let systemPromptText = systemPromptOverride();
+    logFirstResponseTiming({
+      stage: "system prompt built",
+      startedAt: firstResponseTimingStartedAt,
+      runId: params.runId,
+      sessionId: params.sessionId,
+      sessionKey: params.sessionKey,
+      extra: `systemPromptChars=${systemPromptText.length}`,
+    });
 
     let sessionManager: ReturnType<typeof guardSessionManager> | undefined;
     let session: Awaited<ReturnType<typeof createAgentSession>>["session"] | undefined;
@@ -912,6 +1016,14 @@ export async function runEmbeddedAttempt(
         sessionId: params.sessionId,
         cwd: effectiveCwd,
       });
+      logFirstResponseTiming({
+        stage: "session manager prepared",
+        startedAt: firstResponseTimingStartedAt,
+        runId: params.runId,
+        sessionId: params.sessionId,
+        sessionKey: params.sessionKey,
+        extra: `hadSessionFile=${hadSessionFile ? "true" : "false"}`,
+      });
 
       const settingsManager = createPreparedEmbeddedPiSettingsManager({
         cwd: effectiveCwd,
@@ -944,6 +1056,14 @@ export async function runEmbeddedAttempt(
         });
         await resourceLoader.reload();
       }
+      logFirstResponseTiming({
+        stage: "resource loader ready",
+        startedAt: firstResponseTimingStartedAt,
+        runId: params.runId,
+        sessionId: params.sessionId,
+        sessionKey: params.sessionKey,
+        extra: `extensionFactories=${extensionFactories.length}`,
+      });
 
       // Get hook runner early so it's available when creating tools
       const hookRunner = getGlobalHookRunner();
@@ -990,6 +1110,14 @@ export async function runEmbeddedAttempt(
         settingsManager,
         resourceLoader,
       }));
+      logFirstResponseTiming({
+        stage: "agent session created",
+        startedAt: firstResponseTimingStartedAt,
+        runId: params.runId,
+        sessionId: params.sessionId,
+        sessionKey: params.sessionKey,
+        extra: `builtInTools=${builtInTools.length} customTools=${allCustomTools.length}`,
+      });
       applySystemPromptOverrideToSession(session, systemPromptText);
       if (!session) {
         throw new Error("Embedded agent session missing");
@@ -1299,6 +1427,14 @@ export async function runEmbeddedAttempt(
       activeSession.agent.streamFn = wrapStreamFnHandleSensitiveStopReason(
         activeSession.agent.streamFn,
       );
+      logFirstResponseTiming({
+        stage: "stream wrappers ready",
+        startedAt: firstResponseTimingStartedAt,
+        runId: params.runId,
+        sessionId: params.sessionId,
+        sessionKey: params.sessionKey,
+        extra: `transport=${effectiveAgentTransport}`,
+      });
 
       let idleTimeoutTrigger: ((error: Error) => void) | undefined;
 
@@ -1372,6 +1508,14 @@ export async function runEmbeddedAttempt(
         if (limited.length > 0) {
           activeSession.agent.state.messages = limited;
         }
+        logFirstResponseTiming({
+          stage: "session history sanitized",
+          startedAt: firstResponseTimingStartedAt,
+          runId: params.runId,
+          sessionId: params.sessionId,
+          sessionKey: params.sessionKey,
+          extra: `messages=${activeSession.messages.length}`,
+        });
 
         if (params.contextEngine) {
           try {
@@ -1408,6 +1552,14 @@ export async function runEmbeddedAttempt(
             );
           }
         }
+        logFirstResponseTiming({
+          stage: "context engine assembly finished",
+          startedAt: firstResponseTimingStartedAt,
+          runId: params.runId,
+          sessionId: params.sessionId,
+          sessionKey: params.sessionKey,
+          extra: `enabled=${params.contextEngine ? "true" : "false"}`,
+        });
       } catch (err) {
         await flushPendingToolResultsAfterIdle({
           agent: activeSession?.agent,
@@ -1574,6 +1726,13 @@ export async function runEmbeddedAttempt(
         params.replyOperation.attachBackend(queueHandle);
       }
       setActiveEmbeddedRun(params.sessionId, queueHandle, params.sessionKey);
+      logFirstResponseTiming({
+        stage: "embedded subscription ready",
+        startedAt: firstResponseTimingStartedAt,
+        runId: params.runId,
+        sessionId: params.sessionId,
+        sessionKey: params.sessionKey,
+      });
 
       let abortWarnTimer: NodeJS.Timeout | undefined;
       const isProbeSession = params.sessionId?.startsWith("probe-") ?? false;
@@ -1700,6 +1859,13 @@ export async function runEmbeddedAttempt(
           hookRunner,
           legacyBeforeAgentStartResult: params.legacyBeforeAgentStartResult,
         });
+        logFirstResponseTiming({
+          stage: "prompt build hooks finished",
+          startedAt: firstResponseTimingStartedAt,
+          runId: params.runId,
+          sessionId: params.sessionId,
+          sessionKey: params.sessionKey,
+        });
         {
           if (hookResult?.prependContext) {
             effectivePrompt = `${hookResult.prependContext}\n\n${effectivePrompt}`;
@@ -1778,6 +1944,14 @@ export async function runEmbeddedAttempt(
         if (googlePromptCacheStreamFn) {
           activeSession.agent.streamFn = googlePromptCacheStreamFn;
         }
+        logFirstResponseTiming({
+          stage: "prompt cache stream prepared",
+          startedAt: firstResponseTimingStartedAt,
+          runId: params.runId,
+          sessionId: params.sessionId,
+          sessionKey: params.sessionKey,
+          extra: `googleCache=${googlePromptCacheStreamFn ? "true" : "false"}`,
+        });
 
         const routingSummary = describeProviderRequestRoutingSummary({
           provider: params.provider,
@@ -1864,6 +2038,14 @@ export async function runEmbeddedAttempt(
                 ? { root: sandbox.workspaceDir, bridge: sandbox.fsBridge }
                 : undefined,
           });
+          logFirstResponseTiming({
+            stage: "prompt images loaded",
+            startedAt: firstResponseTimingStartedAt,
+            runId: params.runId,
+            sessionId: params.sessionId,
+            sessionKey: params.sessionKey,
+            extra: `images=${imageResult.images.length}`,
+          });
 
           cacheTrace?.recordStage("prompt:images", {
             prompt: effectivePrompt,
@@ -1998,6 +2180,14 @@ export async function runEmbeddedAttempt(
             );
             skipPromptSubmission = true;
           }
+          logFirstResponseTiming({
+            stage: "pre-prompt compaction check finished",
+            startedAt: firstResponseTimingStartedAt,
+            runId: params.runId,
+            sessionId: params.sessionId,
+            sessionKey: params.sessionKey,
+            extra: `skipPrompt=${skipPromptSubmission ? "true" : "false"}`,
+          });
 
           if (!skipPromptSubmission) {
             finalPromptText = effectivePrompt;
@@ -2010,6 +2200,14 @@ export async function runEmbeddedAttempt(
 
             // Only pass images option if there are actually images to pass
             // This avoids potential issues with models that don't expect the images parameter
+            logFirstResponseTiming({
+              stage: "model prompt starting",
+              startedAt: firstResponseTimingStartedAt,
+              runId: params.runId,
+              sessionId: params.sessionId,
+              sessionKey: params.sessionKey,
+              extra: `images=${imageResult.images.length}`,
+            });
             if (imageResult.images.length > 0) {
               await abortable(
                 activeSession.prompt(effectivePrompt, { images: imageResult.images }),
@@ -2017,6 +2215,13 @@ export async function runEmbeddedAttempt(
             } else {
               await abortable(activeSession.prompt(effectivePrompt));
             }
+            logFirstResponseTiming({
+              stage: "model prompt completed",
+              startedAt: firstResponseTimingStartedAt,
+              runId: params.runId,
+              sessionId: params.sessionId,
+              sessionKey: params.sessionKey,
+            });
           }
         } catch (err) {
           yieldAborted =
